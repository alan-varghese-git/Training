r=int(input("Enter the number of rats: "))
u=int(input("Enter the amount of food each rat consumes: "))
n=int(input("Enter the no. of houses: "))
a=list(map(int,input("Enter the amount of food present in each house: ").split()))
t=r*u
f=0
h=0
for i in a:
  f+=i
  h+=1
  if f>=t:
     break
if f<t:
 print(0)
else:
  print(h)
